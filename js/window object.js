// Properties of window object (global) ----------

// height of the window
console.log(innerHeight);  

// width of the window
console.log(innerWidth);

// scroll position in x-dir
console.log(scrollX);

// scroll position in y-dir
console.log(scrollY);

// location
// contains important info about the website
console.table(location);

